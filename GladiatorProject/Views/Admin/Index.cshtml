
@{
    ViewBag.Title = "Index";
}

@if (User.IsInRole("OverLord"))
{
    <h2>Admin Menus</h2>
}
else if (User.IsInRole("Support"))
{
    <h2>Support Menus</h2>
}

<button>
    @Ajax.ActionLink("Players", "PlayerList", new AjaxOptions
   {
       UpdateTargetId = "View",
       InsertionMode = InsertionMode.Replace

   } )
</button>

<button>
    @Ajax.ActionLink("Opponents", "OpponentList", new AjaxOptions
{
UpdateTargetId = "View",
InsertionMode = InsertionMode.Replace

} )
</button>

<button>
    @Ajax.ActionLink("Battles", "BattleList", new AjaxOptions
{
UpdateTargetId = "View",
InsertionMode = InsertionMode.Replace

} )
</button>

<button>
    @Ajax.ActionLink("HighScores", "HighscoreList", new AjaxOptions
{
UpdateTargetId = "View",
InsertionMode = InsertionMode.Replace

} )
</button>

<button>
    @Ajax.ActionLink("Ownerless Gladiators", "Gladiators", new AjaxOptions
{
UpdateTargetId = "View",
InsertionMode = InsertionMode.Replace

} )
</button>

<button>
    @Ajax.ActionLink("Support Requests", "Support", new AjaxOptions
{
UpdateTargetId = "View",
InsertionMode = InsertionMode.Replace

} )
</button>

@if (User.IsInRole("Overlord"))
{
    <button>
        @Html.ActionLink("Create User", "CreateUser")
    </button>

    <button>
        @Ajax.ActionLink("Employees List", "Employees", new AjaxOptions
    {
    UpdateTargetId = "View",
    InsertionMode = InsertionMode.Replace

    } )
    </button>

}


<div id="View">


</div>